apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: ai-guardian
spec:
  to:
    kind: Service
    name: ai-guardian
    weight: 100
  port:
    targetPort: rest
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
