{
  "$schema": "https://raw.githubusercontent.com/itdove/ai-guardian/main/src/ai_guardian/schemas/tray-plugin.schema.json",
  "name": "Quick Actions",
  "scope": "global",
  "items": [
    {
      "label": "Scan Directory...",
      "command": "ai-guardian scan {tray.directory}",
      "type": "terminal",
      "params": [
        {
          "name": "directory",
          "hint": "Directory path to scan for secrets and security issues",
          "default": "."
        }
      ]
    },
    {
      "label": "Sanitize File...",
      "command": "ai-guardian sanitize {tray.file} --summary",
      "type": "terminal",
      "params": [
        {
          "name": "file",
          "hint": "File path to sanitize (text or image)"
        }
      ]
    },
    {
      "label": "Sanitize Directory...",
      "command": "ai-guardian sanitize {tray.directory} --output-dir {tray.output_dir} --summary",
      "type": "terminal",
      "params": [
        {
          "name": "directory",
          "hint": "Directory to sanitize recursively",
          "default": "."
        },
        {
          "name": "output_dir",
          "hint": "Output directory for sanitized files"
        }
      ]
    },
    {
      "label": "Check for Updates",
      "command": "pip index versions ai-guardian",
      "type": "modal"
    },
    {
      "label": "Open Documentation",
      "command": {
        "darwin": "open https://github.com/itdove/ai-guardian#readme",
        "linux": "xdg-open https://github.com/itdove/ai-guardian#readme",
        "default": "echo https://github.com/itdove/ai-guardian#readme"
      },
      "type": "background"
    }
  ]
}
